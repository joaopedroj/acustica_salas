[pytest]
testpaths = tests
pythonpath = scripts scripts/tempo_reverb scripts/sti scripts/claridade scripts/nps
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
